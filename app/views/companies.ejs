<% include header %>

    <h1><%-title %></h1>

    <ul>
	    <% for(var i=0; i < companies.length; i++) {%>
            <li><p><span class="delactivator"><%-companies[i].name%></span>  <span class="delhover">Удалить</span></p></li>
	    <% } %>
    </ul>

    <div>
        <form method="post" action="companies">
            <input name="name"/>
            <input type="submit"/>
        </form>
    </div>

<% include footer %>